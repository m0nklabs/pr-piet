name: PR-Piet

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]
  issue_comment:
    types: [created, edited]

permissions:
  contents: read
  issues: write
  pull-requests: write

# Eén review tegelijk per PR: nieuwe run cancelt de lopende.
concurrency:
  group: pr-piet-${{ github.event.pull_request.number || github.event.issue.number }}
  cancel-in-progress: true

jobs:
  pr-piet:
    if: ${{ github.event.sender.type != 'Bot' }}
    uses: m0nklabs/pr-piet/.github/workflows/reusable-pr-piet.yml@main
    with:
      pr_number: ${{ github.event.pull_request.number || github.event.issue.number }}
      base_branch: ${{ github.event.pull_request.base.ref || '' }}
      enable_tier2: true
      auto_describe: true
      auto_improve: true
      single_call_review: true  # EXPERIMENTEEL: 1 modelcall review+suggesties (fork); terugdraaien: false
      response_language: en-US
    secrets:
      GUARDIAN_API_KEY: ${{ secrets.GUARDIAN_API_KEY }}
